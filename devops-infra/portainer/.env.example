# Portainer Configuration

# Container Settings
TZ=UTC

# Web UI Access
# Default access (first run):
# Access at: http://localhost:9000
# Create admin user on first login

# Edge Agent
# Port 8000 is used for Portainer Edge Agent connections
# Useful for remote agent deployments

# Docker Socket
# Portainer requires access to Docker socket for container management
# Mounted as read-only: /var/run/docker.sock:/var/run/docker.sock:ro

# Data Persistence
# All configuration, user data, and container templates are stored in
# portainer_data volume for persistence across container restarts

# SSL/TLS (Optional)
# Portainer can be configured to use SSL certificates
# Place certificates in /data/certs/ directory if needed
# Reference them in Settings → Security → SSL certificate settings
