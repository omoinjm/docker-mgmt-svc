# Sherlock Username Search Configuration Example

# Service name and container configuration
SERVICE_NAME=sherlock
CONTAINER_NAME=sherlock

# Search timeout (in seconds)
SEARCH_TIMEOUT=10

# Number of retries per site
RETRIES=3

# Optional: Output formats to generate
# Available: json, html, csv
OUTPUT_FORMATS=json,html

# Optional: Specific sites to search (comma-separated)
# If empty, searches all available sites
# SEARCH_SITES=instagram,twitter,github,facebook

# Optional: Proxy settings
# PROXY_URL=http://proxy.example.com:8080
# PROXY_USERNAME=your_username
# PROXY_PASSWORD=your_password

# Optional: Verbosity level
# VERBOSE=false

# Optional: Rate limiting
# RATE_LIMIT_DELAY=1  # Delay in seconds between requests
